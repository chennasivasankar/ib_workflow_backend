import enum


class GroupByEnum(enum.Enum):
    STAGE = "STAGE"
    ASSIGNEE = "ASSIGNEE"
import enum


class GroupByType(enum.Enum):
    STAGE = "STAGE"
    ASSIGNEE = "ASSIGNEE"
    FIELD = "FIELD"
